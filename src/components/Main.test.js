import { render, screen } from "@testing-library/react";
import Main from "./Main";

/*
test("Function updateTimes returns correct state", () => {
  const state = updateTimes();
  expect(state).to;
});

*/
